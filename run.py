from app import app

if __name__ == '__main__':
    # app.run(debug = True) ...remove the 'debug = True' argument from our app.run() because the debug mode has been enabled in the configuration file
    app.run()
